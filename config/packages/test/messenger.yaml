framework:
  messenger:
    transports:
      async: 'sync://'
    routing:
      App\Message\ConvertFileMessage: async

    buses:
      messenger.bus.default:
        middleware:
          - send_message
